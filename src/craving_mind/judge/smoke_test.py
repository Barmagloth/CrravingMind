"""Smoke tests run before each epoch to validate the judge pipeline."""


class SmokeTest:
    """Runs fast sanity checks on judge components before each epoch."""

    def __init__(self, config: dict):
        self.config = config
        # TODO: implement
